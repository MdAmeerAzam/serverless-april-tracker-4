name: SAR 2 True Currency Genesis

on:
  # STOPPED — June 10, 2026. Genesis auto-heal cron disabled.
  # Repo 4 (Currency) halted pending full rebuild. Manual dispatch only.
  workflow_dispatch:

jobs:
  genesis:
    name: Genesis Engine (SAR 2 Auto-Healer)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Run Genesis
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: node scripts/run_currency_genesis.js
